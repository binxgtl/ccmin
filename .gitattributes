# Normalise to LF in the repository. Without this, committing from Windows
# stores CRLF and the bash step in CI fails on Linux runners.
* text=auto eol=lf

# Batch files must keep CRLF to run correctly on Windows.
*.bat text eol=crlf
